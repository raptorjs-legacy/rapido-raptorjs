<c:template
    xmlns:c="core"
    xmlns:w="widgets"
    name="{templateName}"
    params="name,count">
    
    <div class="{cssClassName}"{?ifWidget} w:widget="{widgetName}"{/ifWidget}>
        <h1>{name}</h1>
        Hello $name! You have $count new messages.
    </div>

</c:template>