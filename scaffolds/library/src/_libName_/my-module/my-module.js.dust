define('{libName}/my-module', function(require, exports, module) {
    var logger = module.logger();
    return {
        sayHello: function() {
            logger.debug("Hello World!");
        }
    };
});